# Shared storyboard and optional engines

Read `docs/VIDEO_ENGINE_WORKBENCH.md` from the repository root before extending
this package. Root `AGENTS.md` is still the paid-render authority. This package
currently plans/validates/imports drafts and makes no provider calls. The plan hash
is not spending authorization, and this schema is not the directed-longform schema.
The full production worker adapters are explicitly pending. Preserve those facts
in UI, API and documentation. Keep external runtime dependencies out of this package.
